# FresaStuff-API base URL (default if unset: http://localhost:3000)
VITE_API_URL=https://api.frastas.com

# Data source: 'api' (default — FresaStuff-API, requires login) or 'local'
# (mock database in localStorage, seeded demo data, no backend and no login)
# VITE_DATA_SOURCE=local
